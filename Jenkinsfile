#!/usr/bin/env groovy

properties([
      pipelineTriggers([
            githubPush()
      ]),
      disableConcurrentBuilds()
])

properties([
    buildDiscarder(
        logRotator(
            artifactDaysToKeepStr: '',
            artifactNumToKeepStr: '',
            daysToKeepStr: '',
            numToKeepStr: '30'
            )),
    pipelineTriggers([
        githubPush()
    ]),
    disableConcurrentBuilds()
])

node (){
    try {
             function()
          }

funnction (){
       suma
      }